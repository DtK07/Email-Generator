# Email-Generator

This project is intended to generate the leads for a business to pitch in the product.

Email generator is a python code which generates the possible mail ids of the product once first name, last name and domain is provided as an input from an excel.

Then the generated mail ids are written into a same excel.
